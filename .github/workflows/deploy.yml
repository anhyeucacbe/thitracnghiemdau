runs-on: self-hostedname: Deploy Docker App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: SSH Deploy to Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 192.168.1.15
          username: sinhvien
          password: 123456
          script: |
            cd ~/thitracnghiemdau
            git pull origin main
            docker compose down
            docker compose up -d --build
